name: cashpack
root: ~/src/cashpack/build

windows:
  - build:
      layout: main-horizontal
      panes:
        - vim -c :vsplit $(git ls-files '../*.[ch]' '../*.[ch].in')
        - cmake:
          - git status
