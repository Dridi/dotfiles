name: vmods
root: ~/src

windows:
<% for vmod in [
        'querystring',
        'h2backend',
        'fsdirector',
        'dynamic',
] %>
  - <%= "#{vmod}" %>:
      layout: main-horizontal
      root: ~/src/libvmod-<%= "#{vmod}" %>
      panes:
        - vim -c :vsplit src/*.vcc $(git ls-files '*.[ch]')
        - git status
<% end %>
